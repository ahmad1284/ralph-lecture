# Deprecated

This file is superseded by the codev SPIR structure.

Run instead:
```bash
./loop.sh specify   # lock specs
./loop.sh plan      # create implementation plan
```

Prompts live in `codev/porch/prompts/`.
