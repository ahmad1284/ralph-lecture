# Deprecated

This file is superseded by the codev SPIR structure.

Run instead:
```bash
./loop.sh           # implement (loops until done)
./loop.sh 5         # implement, max 5 iterations
```

Prompts live in `codev/porch/prompts/`.
